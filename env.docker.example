# Backend env (Docker)
MIKROTIK_HOST=172.20.50.6
MIKROTIK_PORT=8729
MIKROTIK_USER=admin
MIKROTIK_PASSWORD=change_me

# If your MikroTik uses a self-signed cert on 8729, easiest path is insecure TLS.
# Set to false if you provide CA PEM below.
MIKROTIK_TLS_INSECURE=true

# Optional: paste CA certificate PEM (single line or multiline). If multiline, keep it quoted in your .env.
MIKROTIK_TLS_CA_PEM=
